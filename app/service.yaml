apiVersion: v1
kind: Service
metadata:
  name: testnodeci
  namespace: production

spec:
  type: NodePort

  selector:
    app: testnodeci

  ports:
  - port: 3000
    targetPort: 3000
    nodePort: 30085